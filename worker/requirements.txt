# Native worker — install on the host itself (not Docker) so torch gets
# the Metal (MPS) backend. ffmpeg must be on PATH (brew install ffmpeg).
#
# EPUB parsing lives in ../core (shared with the API); install it editable:
#   uv pip install -e ../core
chatterbox-tts
# chatterbox's watermarker (perth) needs pkg_resources, removed in setuptools 81+.
setuptools<81
torch
torchaudio
# Used directly to recover chunk boundaries from already-narrated audio, so
# declared rather than leant on as a torch/torchaudio transitive.
numpy
soundfile
psycopg[binary]==3.2.*
